This repo relies on runtime-mounted storage for large artifacts (SQLite DB and Chroma embeddings).

- `.dockerignore` excludes `src/data/embeddings` and `src/data/raw_documents`
- `.gitignore` excludes `src/data/embeddings` and `src/data/raw_documents`

If you add new large datasets, extend both ignore files.

